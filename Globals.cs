namespace LoginApp
{
    /// <summary>
    /// Chứa các biến toàn cục dùng cho toàn ứng dụng, ví dụ như GlobalUserId.
    /// </summary>
    public static class Globals
    {
        /// <summary>
        /// UserId của user đã đăng nhập, gán khi đăng nhập thành công.
        /// </summary>
        public static string GlobalUserId = "";
    }
}